export default {
    theme: {
        extend: {
            fontFamily: {
                sans: ['"Varela Round"', 'sans-serif'], // Use Varela Round as the default sans family
            },
        },
    },
};
